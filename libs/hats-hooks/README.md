# Hats Hooks

Hats Hooks is a set of React hooks for interacting with the SDK and Hats Protocol to fetch data and write data to the chain.

- Update list if held here, assuming will become SDK pkg
- Pass things needed from the context into the hook instead of accessing the context directly (`handlePendingTx`)
